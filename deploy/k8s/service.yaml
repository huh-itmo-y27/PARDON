apiVersion: v1
kind: Service
metadata:
  name: pardon-mlflow-exporter
  namespace: pardon
  labels:
    app.kubernetes.io/name: pardon
    app.kubernetes.io/component: mlflow-exporter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pardon
    app.kubernetes.io/component: mlflow-exporter
  ports:
    - name: metrics
      port: 8010
      targetPort: metrics
